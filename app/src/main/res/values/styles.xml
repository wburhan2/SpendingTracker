<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="SettingTheme" parent="android:Theme.Holo">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
